// Modules-Encapsulated code (onlly share minimum)
const names=require('./4-names');
const sayHi=require('./5-utils')
require('./7-Mind-Grenade')
console.log(names.sal)
sayHi(names.john)
sayHi(names.secret)